name: Deploy EC2 Instances

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Configure AWS Credentials
        uses: actions/aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.ASIASFL3AHUUVP3YEAEZ }}
          aws-secret-access-key: ${{ secrets.wrweORJ4ZY/ghrb+9ioe7zrPgieF5ZAzeGGmuv/c }}
          region: us-east-1 # Replace with your desired region

      - name: Terraform Init
        run: terraform init

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply
